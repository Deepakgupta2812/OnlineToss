# .env.example — copy to .env for local overrides
# No secrets needed — this is a fully client-side app.
# Set VITE_BASE_URL if deploying to a subdirectory (e.g. GitHub Pages repo sub-path)
VITE_BASE_URL=/
